{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block body %}

	{{include ('./nav.html.twig')}}

	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper">
		<h1>Hello
			{{ controller_name }}! ✅</h1>

		<h2>
			{{test}}</h2>


		{# Parcourir un tableau #}
		{% for p in produits %}
			<h3>{{p}}</h3>
		{% endfor %}

		{# Afficher la valeur d'un tableau associatif #}
		<p>Le client s'appelle
			{{client.prenom}}
			{{client.nom}}
			!
		</p>

		<table>
			{% for l in liste %}
				{% for c in l %}
					<tr>
						{{c}}
					</tr>
				{% endfor %}
				<br>
			{% endfor %}
		</table>

		{{liste.client1.prenom}}


		<hr>
		This friendly message is coming from:
		<ul>
			<li>Your controller at
				<code>
					<a href="{{ 'C:/wamp64/www/Test_React/exo_symfony/exo01/src/Controller/HomeController.php'|file_link(0) }}">src/Controller/HomeController.php</a>
				</code>
			</li>
			<li>Your template at
				<code>
					<a href="{{ 'C:/wamp64/www/Test_React/exo_symfony/exo01/templates/home/index.html.twig'|file_link(0) }}">templates/home/index.html.twig</a>
				</code>
			</li>
		</ul>
	</div>
{% endblock %}
